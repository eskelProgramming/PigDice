/**
 * Represents a single game.
 */
class Game {
    /**
     * The players in the game.
     */
    players: Player[];
    /**
     * The player whose turn it is.
     */
    currentPlayer: Player;
}